version: 3

projects:
  - name: 3TierTF
    dir: .
    terraform_version: latest
    autoplan:
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - "modules/**/*.tf"
      enabled: true
    apply_requirements:
      - approved
